{
  "schema_version": "workflow_policy_v1",
  "gate_retry": {
    "default_max_repair_cycles": 3,
    "description": "Default retry budget for repeated deterministic gate failures. Operators may override via CLI when a case needs a different repair loop."
  },
  "rendering": {
    "template_contract_mode": "style_guided",
    "description": "Default PPT rendering treats user templates as style references. Use strict_layout only when the operator explicitly wants placeholder/field structure enforced."
  }
}
